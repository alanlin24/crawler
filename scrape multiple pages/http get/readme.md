this is a multiple pages version of the one in ` ptt / ptt_tennis.py ` /newline

page numbers are explicitly display in the url, so we can scrape through multiple pages simply use for loop to modify the url.
